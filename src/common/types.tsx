export interface CardInterface {
    title: string;
    backgroundColor?: string;
    textColor?: string;
    description?: string;
    date?: string;
    hour?: string;
    value?: string;
}

export interface navbarItemInterface {
    icon: string;
    iconType: string;
    title: string;
    link: string;
}